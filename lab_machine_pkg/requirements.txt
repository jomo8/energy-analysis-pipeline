# Energy Analysis Pipeline — Lab Machine Requirements
# Stages 3 (NLP fine-tuning), 4 (regression), 5 (inference benchmarking)
# Target: Ubuntu 20.04 + Python 3.10 + NVIDIA GPU

# === Scientific stack ===
numpy>=1.24,<2.2
pandas>=2.0,<3.0
pyarrow>=14.0
scikit-learn>=1.3,<2.0

# === Deep learning (Stages 3 and 5) ===
torch>=2.0,<3.0
transformers>=4.41,<5.0

# === Gradient boosting regressors (Stage 4) ===
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# === Required by common.py at module load ===
# common.py does `import requests` for SEC EDGAR helpers.
# Those helpers aren't called by Stages 3/4/5, but the import must succeed.
requests>=2.31

# === Notebook execution (used by run_stage*.sh scripts) ===
jupyter
nbconvert>=7.0
nbformat>=5.9
ipykernel>=6.25

accelerate>=0.26.0
nvidia-ml-py